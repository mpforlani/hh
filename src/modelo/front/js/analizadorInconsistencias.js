/*import * as modulo from './home/abm/crearAbm.js';

console.log(modulo)*/
/*

if (archivoEntrada) {
    const lector = new FileReader();
    lector.readAsText();

    // Cuando se termine de leer el archivo de entrada
    lector.onload = () => {
        const contenido = lector.result;
        console.log(contenido)
        // Escribir el contenido en un archivo de salida
        /*const nombreArchivoSalida = 'archivo-salida.txt';
        const blob = new Blob([contenido], { type: 'text/plain' });
        const enlaceDescarga = document.createElement('a');
        enlaceDescarga.href = URL.createObjectURL(blob);
        enlaceDescarga.download = nombreArchivoSalida;
        enlaceDescarga.click();
    };
} else {
    console.log('No se ha seleccionado ningún archivo de entrada.');
}
*/